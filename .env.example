# Copia este archivo como .env (que SÍ está ignorado por git) si quieres correr local con `netlify dev`.
# En producción, esta variable se configura en el panel de Netlify, NO en el repo.
GEMINI_API_KEY=
